# market-data MCP 架构设计

> 2026-08-08 汇总稿，同日二次修订（2026-08-08 用户定夺：字段级来源链 / 市值计算法 / 默认返回+超长自动导出 / 两份缓存 / EODHD 复权转换 / 北交所链终点报错）。
> 本文档是 market-data MCP 的**架构权威依据**。`docs/market-data设计讨论.md` 已降级为历史过程记录，冲突表述以本文档为准。
> 状态标注：✅ 已实测　⚠️ 有限（降级/仅当前值）　❌ 不可用　❓ 尚未确定/待讨论
> 对应仓库：Ashare-announcements-MCP（与公告 MCP 同仓库，独立包 `market_data_mcp`）。

## 1. 整体定位和说明

### 1.1 功能与定位

三市场（A 股/B 股/港股/美股）**结构化数据**获取工具，与公告阅读 MCP（文档层）并列互补：

|          | 公告 MCP（已有）          | market-data MCP（本设计）                                 |
| -------- | ------------------------- | --------------------------------------------------------- |
| 数据类型 | 公告/提交文档（非结构化） | 行情/财报/指标/概况（结构化）                             |
| 数据源   | 东财公告接口 + SEC EDGAR  | 字段级来源链（EODHD/东财/新浪/腾讯；iFinD+yfinance 仅股本） |
| 消费方式 | 查询/阅读/搜索            | 默认直接返回数据；超长自动导出；指定路径导出              |

个人投资工具。不做汇总文档工具（汇总由用户自己的脚本做）、不做实时行情、不做分钟级数据、不做跨市场科目映射。

### 1.2 工具面（4 工具）

| 工具                         | 参数                                                  | 缓存文件                                       |
| ---------------------------- | ----------------------------------------------------- | ---------------------------------------------- |
| `get_quote`                | code, vars, adjust, start_date, end_date, period, export_path | quote_daily_raw / quote_daily_hfq / shares |
| `get_financial_statements` | code, periods[], statements[]                         | financial_income / balance / cash_flow         |
| `get_financial_ratios`     | code, periods[]                                       | ratios                                         |
| `get_company_profile`      | code, sections[]                                      | profile                                        |

缓存只存行情 raw+hfq 两份（前复权现算不落盘）+ 股本 shares（市值现算不落盘，见 2.4）。

### 1.3 市场与字段范围

- **市场**：A 股（沪/深/创业板/科创板/北交所）、B 股（沪B/深B）、港股、美股正股及ADR（不做 ETF）。
- **行情字段**：除权价（OHLC）、后复权价（OHLC）、前复权价（OHLC，计算）、成交量、成交额、流通市值、总市值。
- **非行情**：财报三表、财务指标、公司概况/分红/盈利预测。

### 1.4 数据源体系（字段为中心，来源可插拔）

**核心原则：每个字段有自己的来源优先级链，不按数据源统一排优先级。数据源不是分析单位，是可替换实现。**

字段链上任一源拿到即填入缓存，全链失败才标注该字段缺失；某字段不稳定只调该字段的链，不动其他字段。

**来源池与能力约束（2026-08 实测）**：

| 数据源 | 参与字段 | 硬限制 | 网络/代理 |
| ------ | -------- | ------ | --------- |
| EODHD | 价格 open/high/low/close（raw）、volume；adjusted_close=前复权口径（实测与新浪 qfq 全段一致） | **无 amount（所有版本）**；**无后复权字段**（可 qfq→hfq 因子转换恢复，见 2.2）；**无北交所**（链终点直接报错）；免费版仅最近 1 年、20 次/天；付费 30+ 年、原生周/月线 | 外网服务，走 Hermes 代理 17891 |
| 东财 | 价格/量/额（行情）、三表/指标/概况（datacenter） | 行情域 push2his 2026-08 持续限流（**全域间歇性**：A/H/US/北交所任何时点都可能失败，回退是常态）；datacenter 域稳定 | 清代理直连 |
| 新浪 | 价格/量/额回退；A 股全能力；港股/美股拉全量本地过滤 | 仅日线（周/月由日线本地聚合）；美股**无 hfq、无 amount** | 清代理直连 |
| 腾讯 | 仅 open/high/low/close 价格回退 | **量/额剔除**（单位不统一：主板/创业板按手、科创板按股，无规律）；无港美股接口；北交所待测 | 清代理直连 |
| iFinD | **仅股本**（THS_DS 近 5 年序列 + THS_BD 单点月频补早期，方案见 §8） | 行情/市值不用（2026-08-08 降级：SDK bug + 付费仅 5 年）；港股代码 `0700.HK`、美股 `AAPL.O`；串行 ~0.5s/请求 | 国内服务，清代理直连 |
| yfinance | 仅港美股**股本回退** | ~10 年上限（2015 底起）；注意库存股（Ordinary = Issued − Treasury） | **必须走代理 127.0.0.1:17891**（直连被 Yahoo 限流，与国内源相反） |

**行情字段默认来源链（2026-08-08 用户拍板）**：

| 字段 | A/B 股 | 港股 | 美股 |
| ---- | ------ | ---- | ---- |
| open/high/low/close（raw） | EODHD → 东财 → 新浪 → 腾讯 | EODHD → 东财 → 新浪 | EODHD → 东财 → 新浪 |
| volume | EODHD → 东财 → 新浪（腾讯剔除） | 同左 | 同左 |
| amount | 东财 → 新浪（EODHD 无、腾讯剔除） | 同左 | **仅东财**（新浪无、EODHD 无、iFinD 已降级） |
| hfq 价格 | 东财 → 新浪 | 东财 → 新浪 | **仅东财直取**；东财失败时由 EODHD qfq 因子转换恢复（见 2.2） |
| 股本 | iFinD（§8 方案）→ 新浪 outstanding_share（上市以来全历史逐日） | iFinD（§8 方案）→ yfinance | iFinD（§8 方案）→ yfinance |
| 市值 | 计算法：股本 × 不复权收盘（见 2.4），不做直接获取 | 同左 | 同左 |
| 财报/指标/概况 | 东财 datacenter | 东财 | 东财（缺失类别 → SEC 公告） |

EODHD 若付费转正为价格链第一顺位；免费版（1 年/20 次每天）期间东财为实际第一顺位。

### 1.5 缓存与更新策略

- 缓存存 `cache/{代码}/` 下，JSON 格式 `{meta, items}`：meta 含 code/market/data_type/source/updated_at/date_range/shape；items 为数据本体。
- **行情只缓存除权（raw）+ 后复权（hfq）两个文件，均为静态历史永不过期**；前复权随分红变动，现算不落盘。
- **市值现算不落盘**（缓存 raw close + 股本，逐日相乘）；股本低频变化，单独缓存 shares（月频/日频并存，同源不冲突）。
- **首次全量、之后增量**：行情拉 last_date 之后新交易日；财报拉新报告期。
- **失败 = 缓存未更新**：直接返回"缓存获取失败，请重试"，不记录失败过程；重试时自动跳过已完成部分。
- 周/月线由日线现算，不落盘（聚合规则：open=周期首日、high=周期最高、low=周期最低、close=周期末日、volume/amount=周期累计；weekly 用 W-FRI、monthly 用 ME）。
- EODHD 场景：hfq 缺失时按 2.2 的因子转换恢复后写入缓存。

### 1.6 输入输出规范

**股票代码（强制后缀，程序按后缀路由市场）**

| 市场               | 格式       | 示例                              | iFinD 转换              | AKShare 转换 |
| ------------------ | ---------- | --------------------------------- | ----------------------- | ------------ |
| 沪（含沪B/科创板） | 6 位 + .SH | 600519.SH / 900901.SH / 688981.SH | 原样                    | 裸码         |
| 深（含深B/创业板） | 6 位 + .SZ | 000001.SZ / 200725.SZ / 300476.SZ | 原样                    | 裸码         |
| 北交所             | 6 位 + .BJ | 920002.BJ                         | 920002.BJ（THS_BD 实测可用 ✅） | 裸码         |
| 港股               | 5 位 + .HK | 00700.HK                          | 0700.HK（4 位带前导零） | 00700        |
| 美股               | 字母 + .US | AAPL.US                           | AAPL.O（.O 后缀）       | AAPL         |

- 后缀大小写不敏感（程序统一处理）。
- **同一个后缀走同一条通道**（如 900901.SH 与 600519.SH 同为沪通道，B 股已实测与 A 股完全同渠道）；**禁止按数字位数/开头猜测市场**（脚本不猜测，AI 负责灵活）。
- 若数据源层提示代码不对，直接报错并给出清晰说明（说明该后缀对应哪个市场、当前源接受的格式），不静默降级重试。
- **北交所**：EODHD 无此市场，价格链走到 EODHD 即失败 → 直接报错"未获取到数据，请重试"，不静默跳过。

**日期**：`YYYY-MM-DD`。日期起点默认空 = 最近 10 个交易日；日期终点默认空 = 当天系统日期。

**返回与导出（2026-08-08 定稿）**：

- `export_path` 空 = **数据直接返回**（rows 数据本体，适合查单日/单周/小范围）。
- 指定 `export_path` = 导出到该路径，返回元信息（路径/条数/范围/单位），不包含 rows。**导出文件格式：CSV**（表格型数据，Excel/pandas 直接可用；meta 信息由返回元信息承担；2026-08-08 定）。
- **超长自动导出防护（token 保护）**：未指定 export_path 但返回行数 **> 200 行** → **自动导出到保留位置**（如 `cache/_auto_export/`），返回导出路径 + 元信息 + 提示（阈值 2026-08-08 用户定）。AI 可方便查单日/单周，误请求大范围也有兜底。

**币种**：行情、成交额、**市值均为原币**（A 股人民币/港股港元/美股美元；市值 = 股本 × 原币收盘），工具不换汇率；需要其他币种由调用方自行换算（2026-08-08 定：市值币种 = 原始币种）。

### 1.7 如何新增

**新增市场**：① 后缀路由表加一行（后缀→渠道）；② 各数据源代码格式转换（iFinD/AKShare 差异表）；③ 字段表逐列补该市场的来源链；④ 按探测纪律实测各源（一次一小点、带超时、先查接口签名）；⑤ 补测试。

**新增字段/变量**：在字段表定义字段名 + 来源链（或派生计算式，如市值=股本×不复权收盘）→ 链上任一环节成功即有值 → 全链失败标注缺失 → 只动该字段，不影响其他字段。

**新增数据源**：实现可插拔 provider（含代码格式转换、代理策略、复权/频率能力声明）→ 接入各字段链 → 探测验证数据一致性（与现有源逐日对比）→ 补测试。

**维护**：接口签名以 `inspect.signature` 实测为准（akshare 版本升级后复测）；复权/频率支持矩阵见 2.2；代理差异（国内源清代理直连、EODHD/yfinance 走代理）见 1.4；NaN 清理必须显式逐值判断（float NaN / pd.NaT 转 None，`df.where(pd.notnull())` 对 float NaN 不生效）。

### 1.8 代码架构（模块化，2026-08-08 讨论定稿）

**三层结构，像拼积木一样组合**：

```
请求模块（叶子）：入参=标准代码+日期范围+档位参数
                 动作=请求源 + 把该档返回的全部列写入缓存（meta 带 source）→ 返回元信息
字段聚合器（中层）：入参=字段名+代码+日期范围
                 动作=读缓存 → 覆盖够直接返回 → 该字段缺 → 按该字段独立源链调请求模块补拉 → 写缓存 → 返回
工具（顶层）：组装多个字段聚合器（get_quote = 除权OHLC + 后复权OHLC + volume + amount + 市值…）
```

- **请求模块粒度 = 源 × 市场 × 复权档**：如 `东财×A/B股×raw`、`东财×A/B股×hfq`、`新浪×港股×raw`、`EODHD×美股×raw`。**不是按单字段切**——各源一次请求物理上返回整组（东财=OHLC+量额+振幅/涨跌幅/换手率；新浪A股=OHLC+量额+outstanding_share+换手率；新浪港股=OHLC+量额；新浪美股=OHLC+量；EODHD=OHLC+adjusted_close+量），按字段切会导致同一请求被多个模块重复执行。
- **代码格式转换放统一适配层（2026-08-08 用户拍板）**：入口只认标准代码（300476.SZ / 00700.HK / AAPL.US），路由层做"后缀→市场→各源格式转换表"（东财裸码/105.AAPL、新浪 sz300476/00700、EODHD 300476.SHE、iFinD 300476.SZ/0700.HK/AAPL.O）；请求模块只接收已转换好的源格式字符串，内部不再做任何格式处理。新增数据源 = 写请求模块 + 转换表加一行。
- **宽写窄读**：请求模块把该档全部列一次写入缓存；字段层按列名从缓存消费（如股本字段消费新浪A股模块写入的 outstanding_share），避免重复请求。
- **字段链按字段独立定义**：可得性不同则链不同（美股 amount 链=仅东财；美股 OHLC 链=EODHD→东财→新浪）。字段聚合器补拉时按自己链调对应请求模块。
- **缺字段补拉语义（决策 A，2026-08-08 用户拍板）**：字段缺数据时，字段链总是从第一源开始重试，**接受对刚失败源的重复请求**（东财限流是间歇性的，重试可能撞上恢复窗口）；**不记录失败状态**（维持"失败=缓存未更新，提示重试"的简单策略）。
- **模块接口契约（决策 A，2026-08-08 用户拍板）**：全部**结构化返回，不抛异常**——请求模块内部 try/except，统一返回 `{ok, source, path, new_items, date_range, error}`；字段聚合器根据 ok 决定是否试下一源（链式尝试逻辑在聚合器内）。失败原因可读（如"东方财富请求超时"），便于 notes 记录与测试。
- **运行日志（审计，2026-08-08 用户拍板）**：**每次对上游的请求都记一条日志**——时间、请求什么源的什么数据（市场/代码/接口/字段/档位）、运行是否成功、报错信息、耗时。目的：评估数据源可靠性（哪个源稳定、哪个常挂）。日志是 append-only 审计，**不参与任何决策逻辑**（与"不记录缓存失败状态"不冲突：日志≠状态）。落点：`logs/requests.jsonl`（JSON Lines，一行一条；建议字段：ts/source/market/code/api/fields/adjust/start/end/ok/elapsed/error）。
- **派生字段无请求模块**：市值 = 字段聚合器读缓存（股本 + raw close）现算，不落盘。
- **调整顺序/新增来源/修 bug**：改字段链注册或单个请求模块，不影响其他模块。

## 2. 行情获取工具 `get_quote`

### 2.1 工具输入

| 参数        | 必填 | 默认  | 说明                                                                    |
| ----------- | ---- | ----- | ----------------------------------------------------------------------- |
| code        | ✅   | —    | 带后缀代码（见 1.6）                                                    |
| vars        | —   | close | 返回字段列表，可多选；date 恒保留。可选：open/high/low/close/volume/amount/turnover/outstanding_share/total_market_cap/float_market_cap 等（历史代码曾用 `fields`，作废） |
| adjust      | —   | raw   | 复权方式：raw=除权价 / hfq=后复权 / qfq=前复权                          |
| start_date  | —   | 空    | 日期起点，空 = 最近 10 个交易日                                         |
| end_date    | —   | 空    | 日期终点，空 = 当天系统日期                                             |
| period      | —   | daily | daily/weekly/monthly；周/月线由日线现算不落盘                           |
| export_path | —   | 空    | 空 = 直接返回数据（超长自动导出，见 1.6）；指定 = 导出并返回元信息      |

### 2.2 复权口径与获取逻辑

- **a. 除权价**：高开低收四项**一次请求打包获取**（不单独获取单项），需要什么从缓存读取/下载。来源链见 1.4 字段表。
- **b. 后复权价**：**直接使用数据源返回的数据，不做任何处理**（不换汇率、不做任何改动）。东财 hfq / 新浪 hfq 均为提供商原值。
  - **EODHD 场景（无 hfq 字段）**：EODHD 只有 raw + adjusted_close（前复权口径）。当需要 hfq 而 EODHD 是唯一可得源时，用**因子转换**恢复：
    - 取与 EODHD 重叠日期上已有的 hfq（来自东财/新浪或缓存），计算最近一个复权因子 `factor = hfq(参考日) / qfq(参考日)`；
    - `hfq(t) = qfq(t) × factor`，转换结果写入缓存；
    - 参考日取最近有 hfq 的重叠交易日；参考日之后发生除权事件时因子失效，用最新参考日重算。
  - 美股 hfq 是唯一无直取回退的字段（新浪无、EODHD 无），东财限流时靠上述转换从 EODHD qfq 恢复。
- **c. 前复权价**：**本地计算，不缓存**。`前复权(t) = 后复权(t) × (最新不复权收盘 / 最新后复权收盘)`，除权因子取同一交易日（日期对齐），随分红自动更新。
- 请求前复权时：自动获取后复权 + 除权的高开低收到缓存，再从缓存读取并计算前复权。
- 缓存只存不复权（raw）+ 后复权（hfq）两个文件，均为静态历史永不过期。

**复权/频率能力矩阵（akshare 1.18.83 + iFinD + EODHD 实测，2026-08）**

| 接口                                | 源         | raw      | hfq                          | qfq                          | weekly/monthly | start/end     |
| ----------------------------------- | ---------- | -------- | ---------------------------- | ---------------------------- | -------------- | ------------- |
| EODHD eod/{code}                    | EODHD      | ✅       | ❌ → qfq 因子转换恢复        | ✅ adjusted_close（=qfq 口径） | ✅ 原生        | ✅            |
| stock_zh_a_hist / hk_hist / us_hist | 东财       | ✅       | ✅                           | ✅                           | ✅ 原生        | ✅            |
| stock_zh_a_daily                    | 新浪(A股)  | ✅       | ✅                           | ✅                           | ❌ 无参数      | ✅            |
| stock_hk_daily                      | 新浪(港股) | ✅       | ✅（1.18.81 复测有效）       | ✅                           | ❌ 无参数      | ❌ 拉全量过滤 |
| stock_us_daily                      | 新浪(美股) | ✅       | ❌ 实测 TypeError            | ✅                           | ❌ 无参数      | ❌ 拉全量过滤 |
| 腾讯 stock_zh_a_hist                | 腾讯       | ✅       | ✅                           | ✅                           | ✅             | ✅            |
| iFinD THS_HistoryQuotes             | iFinD      | —        | —                            | —                            | —              | —             |

iFinD 行情已降级（仅股本），不再进入行情链。新浪回退且请求周/月线：本地把日线聚合成周/月线（规则见 1.5），返回 `notes` 提示"新浪接口仅提供日线，weekly/monthly 由日线本地聚合"。

### 2.3 行情字段与可得性

| 字段                | 含义           | 获取方式       | A股/B股                          | 港股                             | 美股                                                              |
| ------------------- | -------------- | -------------- | -------------------------------- | -------------------------------- | ----------------------------------------------------------------- |
| open/high/low/close | 除权价 4 项    | 核心组一次打包 | EODHD✅ 东财✅ 新浪✅ 腾讯✅      | EODHD✅ 东财✅ 新浪✅            | EODHD✅ 东财✅ 新浪✅                                             |
| volume              | 成交量         | 核心组打包     | EODHD✅ 东财✅ 新浪✅（腾讯剔除）| EODHD✅ 东财✅ 新浪✅            | EODHD✅ 东财✅ 新浪✅                                             |
| amount              | 成交额（原币） | 核心组打包     | 东财✅ 新浪✅（EODHD❌ 腾讯❌）   | 东财✅ 新浪✅（EODHD❌）         | **仅东财✅**（新浪❌ EODHD❌ iFinD 已降级）                         |
| total_market_cap    | 总市值         | 计算法（2.4） | iFinD股本 → 新浪股本（估算值）   | iFinD股本 → yfinance股本（估算值） | iFinD股本 → yfinance股本（估算值）                                |
| float_market_cap    | 流通市值       | 计算法（2.4） | iFinD股本 → 新浪 outstanding_share（估算值） | iFinD floating_shares（估算值） | iFinD floating_shares（估算值）                                   |

### 2.4 市值（计算法，2026-08-08 定稿）

- **定案：市值 = 股本 × 当日不复权收盘价（逐日对齐 K 线），现算不落盘；不做 iFinD market_value 直接获取。**
- 理由：已有股本和行情价格即可自算，无需 iFinD 提取；估算值统一标注"总市值/流通市值为估算值（股本 × 不复权收盘价）"。
- **股本来源链**：
  - A/B 股：iFinD（THS_DS 近 5 年序列 + THS_BD 单点月频补早期，方案见数据源文档 §8）→ 新浪日线 `outstanding_share`（上市以来全历史逐日，真实序列非当前值）。
  - 港股/美股：iFinD（同 §8 方案）→ yfinance（年度 5 期/季度 2 年/事件点 10 年；必须走代理 17891；注意库存股）。
- **流通市值**：A/B 股用新浪 outstanding_share；港美股用 iFinD floating_shares（近 5 年序列 + 早期单点月频）；yfinance 无 float 历史序列（仅当前值可作参考）。
- **不从财报提取季度股本反推历史股本**（2026-08-08 决策：复杂且易错，后面确有需要再处理；A 股资产负债表 SHARE_CAPITAL 数据存在但暂不接入）。
- 股本低频变化（回购/增发/解禁/送转才变），月频采样足够；缓存带 source 元数据，不跨源填充（防 yfinance 扣库存股与 iFinD 口径差异）。
- 币种：**原始币种**（股本 × 原币收盘；A 股人民币/港股港元/美股美元，2026-08-08 定），不做汇率换算。

### 2.5 返回结构

- 默认（export_path 空）：`{ok, market, code, start, end, adjust, period, vars, source, notes, rows}`——rows 为数据本体（date 恒在，其余按 vars 与市值列）；**超阈值自动导出**（见 1.6）时返回 `{ok, auto_exported: true, path, total_items, date_range, source, notes}`。
- 指定 export_path：`{ok, path, total_items, date_range, source, notes}`，不含 rows。
- `notes`：非空 = 降级/聚合/估算说明（如"新浪接口仅提供日线，weekly/monthly 由日线本地聚合"、"新浪美股接口不支持后复权，已返回前复权数据"、"市值为估算值"、"已自动导出到 {path}"）；AI 必须感知。
- `source`：EODHD / eastmoney / sina——告知实际取数路径，AI 据此判断字段完整性（东财路径有 amplitude/quote_change/change/turnover，新浪路径没有）。

## 3. 财报获取工具 `get_financial_statements`

### 3.1 定位

三市场原始三表（利润表/资产负债表/现金流量表）获取。**统一形状、不改科目名**：

```json
items: [{"report_date", "item_name", "amount", "source"}, ...]
```

- 科目名原样保留：A 股宽表内部转长表时科目名不翻译；港股/美股返回什么科目就叫什么科目。
- **不做跨市场科目映射**（映射=自己猜=iFinD 的坑；跨市场比较由 AI/用户脚本处理）。
- 数值单位：**原始货币（元）**——人民币/港元/美元原币数值，不做单位换算（2026-08-08 定）；返回/导出时在元信息中标注币种。

### 3.2 参数

`code`（必填）、`periods[]`（报告期年份列表，匹配该年所有季度，空=全部）、`statements[]`（income/balance/cash_flow，空=三表全取）。

### 3.3 数据可得性

| 市场   | 数据源          | 形态                                       | 状态 |
| ------ | --------------- | ------------------------------------------ | ---- |
| A 股   | 东财 datacenter | 宽表（资产负债表实测 48 报告期 × 319 列） | ✅   |
| 港股   | 东财 datacenter | 长表                                       | ✅   |
| 美股   | 东财 datacenter | 长表（资产负债表实测 41 科目，无股本科目） | ✅   |

- 增量：按报告期，首次全量、之后拉新报告期。
- 东财 datacenter 域稳定（2026-08 实测不受行情域限流影响）。

## 4. 衍生数据获取工具 `get_financial_ratios`

### 4.1 定位

财务衍生指标（ROE/毛利率/负债率等），**原始返回、东财列名原样**（如 ROEJQ=加权ROE、ROEKCJQ=扣非ROE、MLR=毛利率、EPSJB=基本EPS、BPS=每股净资产、TOTALOPERATEREVE=营业总收入、PARENTNETPROFIT=归母净利润）。命名依据：行业标准把原始三表叫 Statements、衍生比率叫 Ratios，不叫 indicators（易误解为技术指标 MACD/RSI）。

### 4.2 参数

`code`（必填）、`periods[]`（年份列表，空=全部）。

### 4.3 数据可得性

| 市场   | 数据源 | 形态                 | 状态      |
| ------ | ------ | -------------------- | --------- |
| A 股   | 东财   | 多期，141 列         | ✅        |
| 港股   | 东财   | **仅最新 TTM** | ⚠️ 有限 |
| 美股   | 东财   | 年报多期             | ✅        |

## 5. 公司信息获取工具 `get_company_profile`

### 5.1 定位

公司概况/分红/盈利预测，一个缓存文件（profile.json），`sections` 可选（profile/dividends/forecast，空=全部）。

### 5.2 参数

`code`（必填）、`sections[]`（profile/dividends/forecast，空=全部）。

### 5.3 数据可得性

| 内容                 | A 股                                                      | 港股      | 美股           |
| -------------------- | --------------------------------------------------------- | --------- | -------------- |
| 概况                 | ✅ 巨潮（公司名称/英文名/曾用简称/代码/入选指数）         | ✅        | ❌ → SEC 公告 |
| 分红                 | ✅ 历史记录（报告期/披露日/送转/现金分红比例/股息率/EPS） | ✅        | ❌ → SEC 公告 |
| 盈利预测             | ✅ 一致预期                                               | ✅ 分券商 | ❌             |

美股缺失类别（概况/分红/十大股东/主营构成/IPO）需从公告原文获取——对应公告 MCP（美股走 SEC EDGAR）。

## 6. 明确不做（边界）

- ❌ 不做汇总文档工具（用户自己写脚本汇总）
- ❌ 不做跨市场科目映射（科目名原样）
- ❌ 不做实时行情（只做历史日/周/月线）
- ❌ 不做分钟级数据
- ❌ 不做汇率换算（原币返回）
- ❌ 不记录缓存失败状态（失败即提示重试）
- ❌ 不从财报提取季度股本反推历史股本（复杂易错，后需再处理）

## 7. 待办

- [ ] **待决：跨源 hfq 口径差异的处理方式（2026-08-08 按下不表）**：0700.HK 实测东财 hfq=2683.83 vs 新浪 hfq=2672.93（差 0.41%），各源后复权因子基准/算法略异。数据源切换会带来价格波动不一致，无法区分市场波动与数据源切换影响（趋势可用、细节不可用）。候选：按源分文件+主源续拉+备源补缺口+notes 标注 vs 主源粘性（主源失败直接报错）vs 其他。用户暂未定，实现时先按"缓存不跨源混用"的最小落地处理。
- [ ] **接口返回格式表**：主要接口已实测填充（见数据源文档 §9）；东财美股 `stock_us_hist` 列名待限流恢复后复测
- [ ] 北交所行情：腾讯原始接口 ✅（qfq/hfq 有数据；akshare 1.18.83 包装 bug 见数据源文档 §9）；东财复测待限流恢复
- [x] 北交所股本 iFinD 代码格式：`920002.BJ` THS_BD 实测可用（2026-08-08）✅
- [x] EODHD qfq→hfq 因子转换端到端验证：300476.SHE 28 重叠日误差 ≤0.0006%（2026-08-08）✅
- [ ] EODHD 付费版评估（若转正：30+ 年全历史；fundamentals 三表是否采用）——暂缓
- [x] 市值估算端到端验证：AAPL 2010 至今 139 月频点全成功、0700.HK 2004 起抽样（2026-08-08）✅
- [ ] MCP 注册（Hermes config + 能力库）与 export.py 双包导出

> 已定稿（2026-08-08）：导出文件格式 = CSV；财报数值单位 = 原始货币（元）；市值币种 = 原始币种；超长自动导出阈值 = 200 行。
