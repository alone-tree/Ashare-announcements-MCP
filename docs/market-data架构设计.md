# market-data MCP 架构设计

> **本文件记录什么（决策中枢）**：market-data MCP 的全部决策——数据源选择与来源链、复权口径、缓存策略、代码架构、输入输出规范、股本方案、历史关键决策、待办。
>
> **本文件不记录什么（客观能力）**：各数据源支持哪些市场/字段/参数、返回格式、单位、限制等客观事实一律写入 `字段与数据源支持情况.md`，本文件只引用不复述（如"客观可得性见数据源文档 §2"）。新增内容前先判断归属：**决策 → 本文件；客观事实 → 数据源文档**。
>
> 2026-08-08 汇总稿，2026-08-09 行情单源化定稿。`docs/market-data设计讨论.md` 已降级为历史过程记录，冲突表述以本文档为准。
> 状态标注：✅ 已定稿　⚠️ 有限　❌ 不做　❓ 待决
> 对应仓库：Ashare-announcements-MCP（与公告 MCP 同仓库，独立包 `market_data_mcp`）。

## 1. 整体定位和说明

### 1.1 功能与定位

三市场（A 股/B 股/港股/美股）**结构化数据**获取工具，与公告阅读 MCP（文档层）并列互补：

|          | 公告 MCP（已有）          | market-data MCP（本设计）                                 |
| -------- | ------------------------- | --------------------------------------------------------- |
| 数据类型 | 公告/提交文档（非结构化） | 行情/财报/指标/概况（结构化）                             |
| 数据源   | 东财公告接口 + SEC EDGAR  | 行情新浪单源；财报东财 datacenter；股本 iFinD（见 1.4）   |
| 消费方式 | 查询/阅读/搜索            | 默认直接返回数据；超长自动导出；指定路径导出              |

个人投资工具。不做汇总文档工具（汇总由用户自己的脚本做）、不做实时行情、不做分钟级数据、不做跨市场科目映射。

### 1.2 工具面（5 工具）

| 工具                         | 参数                                                  | 缓存文件                                       |
| ---------------------------- | ----------------------------------------------------- | ---------------------------------------------- |
| `get_quote`                | code, vars, adjust, start_date, end_date, period, export_path | **字段级 9 个 json**：open/high/low/close/close_hfq/volume/amount/total_shares/floating_shares |
| `get_financial_statements` | code, amount_basis, statements, items, report_types, start/end, versions | financial_income / balance / cash_flow         |
| `get_data_catalog`         | code, statements                                      | 只读对应公司三表缓存，不增加文件               |
| `get_financial_ratios`     | code, periods[]                                       | ratios                                         |
| `get_company_profile`      | code, sections[]                                      | profile                                        |

缓存只存行情 raw+hfq 两份（前复权现算不落盘）+ 股本 shares（市值现算不落盘，见 2.4）。

### 1.3 市场与字段范围

- **市场**：A 股（沪/深/创业板/科创板/北交所）、B 股（沪B/深B）、港股、美股正股及ADR（不做 ETF）。
- **行情字段**：除权价（OHLC）、后复权价（OHLC）、前复权价（OHLC，计算）、成交量、成交额、流通市值、总市值。
- **非行情**：财报三表、财务指标、公司概况/分红/盈利预测。

### 1.4 数据源体系（决策）

**核心原则（2026-08-08 拍板）**：字段为中心、来源可插拔；前复权本地计算不依赖源；市值走计算法（股本 × 不复权收盘，不做直接获取）。**不按数据源统一排优先级——每个字段有自己的来源链**。

**行情单源化（2026-08-09 定稿）**：除权价与后复权均单源无回退，**避免不同源数据混杂**——raw 是事实价格本可多源，但东财行情域 2026-08 持续限流、EODHD 免费版仅 1 年、腾讯量额不可靠，单源新浪最简单可靠；后复权跨源口径实测不一致（600519 iFinD↔新浪比值恒定、0700.HK 比值漂移不可混用），必须单源。

**来源池角色（决策）**：

| 数据源 | 角色（参与什么） | 不参与什么 | 代理策略 |
| ------ | ---------------- | ---------- | -------- |
| 新浪（AKShare） | **raw 高开低收单源（A/港/美/北交所）**；**A/港股 hfq close 单源** | 美股 hfq（无此能力）、美股 amount（无此能力） | 清代理直连 |
| iFinD | **股本**（全市场）+ **美股 hfq close**（近 5 年 THS_DS `107,OC` + 更早 THS_BD `107,OC` 单点补全）+ **美股 amount**（近 5 年 THS_DS `amt,OC` + 更早 THS_BD 单点逐日补全，2026-08-09 用户提供官方公式） | 行情 raw/量额（A/港） | 国内服务，清代理直连 |
| 东财（AKShare） | 财报/指标/概况（datacenter） | 行情 raw/hfq、美股 amount（2026-08-09 起，行情域限流不再影响主要链路；美股 amount 废弃） | 清代理直连 |
| EODHD | ~~价格链~~（2026-08-09 **退出行情链**，保留为知识；付费转正再议） | — | 外网走代理 17891 |
| 腾讯（AKShare） | ~~OHLC 价格回退~~（2026-08-09 **退出行情链**，保留为知识） | 量额（单位不稳定，2026-08-08 扩测 7 家定论） | 清代理直连 |
| yfinance | ~~仅港美股股本回退~~（2026-08-09 **不接入**，保留为知识——唯一源原则：不同源口径可能不一致，回退=重复请求浪费） | 行情 | **必须走代理 17891**（直连被 Yahoo 限流，与国内源相反） |

**行情字段来源链（2026-08-09 定稿）**：

| 字段 | A/B 股 | 港股 | 美股 |
| ---- | ------ | ---- | ---- |
| open/high/low/close（raw） | **新浪（单源，无回退）** | **新浪（单源，无回退）** | **新浪（单源，无回退）** |
| volume | 新浪 | 新浪 | 新浪 |
| amount | 新浪 | 新浪 | **iFinD 单源**（`amt,OC`：近 5 年 THS_DS 序列 + 更早 THS_BD 单点按交易日历逐日补全；单点非交易日返回空值故必须严格交易日历；东财 amount 废弃——限流从未实测成功） |
| hfq 价格 | **新浪（仅存 close，无回退）** | **新浪（仅存 close，无回退）** | **iFinD THS_DS `107,OC`=分红再投（近 5 年 + 更早单点 THS_BD 补全，全程分红再投）** |
| 股本 | iFinD（§7 方案）；**A 股流通股本=新浪 outstanding_share**（2026-08-09 唯一源拍板，不做回退） | iFinD（§7 方案） | iFinD（§7 方案） |
| 市值 | 计算法：股本 × 不复权收盘（见 2.4），不做直接获取 | 同左 | 同左 |
| 财报/指标/概况 | 东财 datacenter | 东财 | 东财（缺失类别 → SEC 公告） |

**字段链原则**：链上任一源拿到即填入缓存，全链失败才标注该字段缺失；某字段不稳定只调该字段的链，不动其他字段。

### 1.5 缓存与更新策略

- 缓存存 `cache/{代码}/` 下，JSON 格式 `{meta, items}`：meta 含 code/market/field/source/updated_at/date_range/shape；**items 为 `[{date, value, source}]`——source 是每条数据的来源追踪标记，每字段同日期仅一条记录（唯一源保证，2026-08-09 用户拍板）**。**探测元信息**：`verified_until`（盘后探测确认的日期上界，周五~周日延伸）+ `verified_at`（秒级）+ `last_probe {state: intraday|closed, date}`（探测状态机，见 1.8）。
- **行情缓存为字段级独立 json（2026-08-09 用户拍板）**：高开低收原值 open/high/low/close、后复权 close_hfq（仅 close）、成交额 amount、成交量 volume、总股本 total_shares、流通股本 floating_shares——9 个文件；均为静态历史永不过期。**前复权随分红变动，现算不落盘**。
- **市值现算不落盘**（close.json × 股本字段，逐日前置填充相乘）；股本低频变化，total_shares/floating_shares 月频/日频并存，同源不冲突。
- **行情首次全量、之后增量**：拉取 `last_date` 之后的新交易日。
- **财报固定 30 天新鲜期**：首次或过期时重新拉取三表全历史并做内容哈希比较；`force_refresh=true` 可强制刷新。无缓存且刷新失败直接报错；已有完整缓存时返回上一批并明确标记 `stale/refresh_error`。
- 周/月线由日线现算，不落盘（聚合规则：open=周期首日、high=周期最高、low=周期最低、close=周期末日、volume/amount=周期累计；weekly 用 W-FRI、monthly 用 ME）。

### 1.6 输入输出规范

**股票代码（强制后缀，程序按后缀路由市场）**

| 市场               | 格式       | 示例                              | iFinD 转换              | AKShare 转换 |
| ------------------ | ---------- | --------------------------------- | ----------------------- | ------------ |
| 沪（含沪B/科创板） | 6 位 + .SH | 600519.SH / 900901.SH / 688981.SH | 原样                    | 裸码         |
| 深（含深B/创业板） | 6 位 + .SZ | 000001.SZ / 200725.SZ / 300476.SZ | 原样                    | 裸码         |
| 北交所             | 6 位 + .BJ | 920002.BJ                         | 920002.BJ（实测可用）   | 裸码         |
| 港股               | 5 位 + .HK | 00700.HK                          | 0700.HK（4 位带前导零） | 00700        |
| 美股               | 字母 + .US | AAPL.US                           | 探测 `.O`/`.N`（见 2.2） | AAPL         |

- 后缀大小写不敏感（程序统一处理）。
- **同一个后缀走同一条通道**（如 900901.SH 与 600519.SH 同为沪通道，B 股已实测与 A 股完全同渠道）；**禁止按数字位数/开头猜测市场**（脚本不猜测，AI 负责灵活）。
- 若数据源层提示代码不对，直接报错并给出清晰说明（说明该后缀对应哪个市场、当前源接受的格式），不静默降级重试。

**日期**：`YYYY-MM-DD`。日期起点默认空 = 最近 10 个交易日；日期终点默认空 = 当天系统日期。**`start_date` 特殊值 `"all"`（2026-08-10 增加，为 chart MCP 的全量需求）**：起点不约束——end 留空 = 返回全部数据；end 指定 = 只取 ≤end 的早期数据。返回字段 `start` 显示 `"all"`、`end` 显示 `"latest"` 或指定日期；内部用哨兵起点 `1990-01-01` 触发全量覆盖判定与补拉（数据起点不受此限制，以实际上市/可用为准）。

**返回与导出（2026-08-08 定稿）**：

- `export_path` 空 = **数据直接返回**（rows 数据本体，适合查单日/单周/小范围）。
- 指定 `export_path` = 导出到该路径，返回元信息（路径/条数/范围/单位），不包含 rows。**导出文件格式：CSV**。
- **超长自动导出防护（token 保护）**：未指定 export_path 但返回行数 **> 200 行** → **自动导出到保留位置**（如 `cache/_auto_export/`），返回导出路径 + 元信息 + 提示（阈值 2026-08-08 用户定）。

**币种**：行情、成交额、**市值均为原币**（A 股人民币/港股港元/美股美元；市值 = 股本 × 原币收盘），工具不换汇率；需要其他币种由调用方自行换算（2026-08-08 定：市值币种 = 原始币种）。

### 1.7 如何新增

**新增市场**：① 后缀路由表加一行（后缀→渠道）；② 各数据源代码格式转换（iFinD/AKShare 差异表，见数据源文档）；③ 字段表逐列补该市场的来源链；④ 按探测纪律实测各源（一次一小点、带超时、先查接口签名）；⑤ 补测试。

**新增字段/变量**：在字段表定义字段名 + 来源链（或派生计算式，如市值=股本×不复权收盘）→ 链上任一环节成功即有值 → 全链失败标注缺失 → 只动该字段，不影响其他字段。

**新增数据源**：实现可插拔 provider（含代码格式转换、代理策略、复权/频率能力声明）→ 接入各字段链 → 探测验证数据一致性（与现有源逐日对比）→ 补测试。

**维护**：接口签名以 `inspect.signature` 实测为准（akshare 版本升级后复测）；代理差异（国内源清代理直连、yfinance 走代理）见 1.4；NaN 清理必须显式逐值判断（float NaN / pd.NaT 转 None，`df.where(pd.notnull())` 对 float NaN 不生效）。

### 1.8 代码架构（模块化，2026-08-08 讨论定稿）

**三层结构，像拼积木一样组合**：

```
请求模块（叶子）：入参=标准代码+日期范围+档位参数
                 动作=请求源 + 把该档返回的全部列写入缓存（meta 带 source）→ 返回元信息
字段聚合器（中层）：入参=字段名+代码+日期范围
                 动作=读缓存 → 覆盖够直接返回 → 该字段缺 → 按该字段独立源链调请求模块补拉 → 写缓存 → 返回
工具（顶层）：组装多个字段聚合器（get_quote = 除权OHLC + 后复权OHLC + volume + amount + 市值…）
```

- **请求模块粒度 = 源 × 市场 × 复权档**：如 `新浪×A/B股×raw`、`新浪×港股×raw`、`iFinD×美股×hfq`。**不是按单字段切**——各源一次请求物理上返回整组（新浪A股=OHLC+量额+outstanding_share+换手率；新浪港股=OHLC+量额；新浪美股=OHLC+量；iFinD THS_DS=指标序列），按字段切会导致同一请求被多个模块重复执行。
- **代码格式转换放统一适配层（2026-08-08 用户拍板）**：入口只认标准代码（300476.SZ / 00700.HK / AAPL.US），路由层做"后缀→市场→各源格式转换表"（新浪 sz300476/00700、iFinD 300476.SZ/0700.HK/AAPL.O）；请求模块只接收已转换好的源格式字符串，内部不再做任何格式处理。新增数据源 = 写请求模块 + 转换表加一行。
- **拉到什么写什么（2026-08-09 用户拍板，取代"宽写窄读"）**：请求模块一次上游请求返回整组数据，**按字段拆分写入各自独立 json**（新浪A股一次写 7 字段含 floating_shares、美股 5 字段无 amount、股本 2 字段）；字段层按字段名从缓存消费，避免重复请求。
- **字段链按字段独立定义（2026-08-09 用户拍板：每字段唯一源，先不做来源回退）**：不同源数据可能不完全一致，混放需额外处理，且回退=重复请求浪费——**每个字段 × 市场只有一个最合适的数据源**（美股 amount=ifind、美股 OHLC=新浪、股本=ifind 等，见 1.4/2.3），单一来源不够再讨论增加。字段聚合器按字段调对应请求模块。
- **缺字段补拉语义（决策 A，2026-08-08 用户拍板）**：字段缺数据时，字段链总是从第一源开始重试，**接受对刚失败源的重复请求**（东财限流是间歇性的，重试可能撞上恢复窗口）；**不记录失败状态**（维持"失败=缓存未更新，提示重试"的简单策略）。
- **模块接口契约（决策 A，2026-08-08 用户拍板；2026-08-09 字段级后更新）**：全部**结构化返回，不抛异常**——请求模块内部 try/except，统一返回 `{ok, source, fields, error, notes}`（fields 为本次更新的字段名 → date_range 映射）；字段聚合器据 ok 与 fields 确认字段已更新（唯一源，无链式回退）。失败原因可读（如"东方财富请求超时"），便于 notes 记录与测试。
- **运行日志（审计，2026-08-08 用户拍板）**：**每次对上游的请求都记一条日志**——时间、请求什么源的什么数据（市场/代码/接口/字段/档位）、运行是否成功、报错信息、耗时。目的：评估数据源可靠性。日志是 append-only 审计，**不参与任何决策逻辑**。落点：`logs/requests.jsonl`（JSON Lines，一行一条；建议字段：ts/source/market/code/api/fields/adjust/start/end/ok/elapsed/error）。
- **缓存覆盖判定（2026-08-08 用户拍板；2026-08-09 探测验证 + 状态机）**：**必须缓存完整覆盖请求日期段才算够**——`c_start ≤ start 且 c_end ≥ end`（双向段覆盖）才直接返回缓存；**末尾探测验证（无交易日历）**：补拉 `[c_end+1, end]` 后若上游返回末日 < end（end 之前无更多已收盘数据）→ 记录探测结果：
  - **盘后探测**（`routing.is_market_closed`：A/BJ 北京时间 ≥15:00、HK ≥16:00、US 美东时间 ≥16:00 含夏令时；周末视为已收盘）→ 写 meta `verified_until`（日期）+ `verified_at`（秒级）+ `last_probe={state:closed}`；**周五~周日探测自动延伸到所在周末周日**（周末确定无数据）→ 判定时 `verified_until ≥ end` 视为覆盖（周末/节假日休市场景零请求）
  - **盘中探测**（未收盘）→ 只写 `last_probe={state:intraday, date}`，不写 verified_until（当日收盘后可能出数据，不能被误缓存）→ **盘中续探**：上次 intraday 且与本次请求同一天且当前仍未收盘 → 视为覆盖（盘中"最新收盘日"不会变，不重复请求）；本次盘后 → 不覆盖 → 补拉当天数据
  - 跨天自然失效（每天可能出新数据）：下次请求 end 超出 verified_until/last_probe.date → 补拉
  其他情况缺什么补什么：终点不足 → 增量补拉（c_end+1 到 end）；起点不足 → 按字段链补早期段；补拉后仍覆盖不了的部分 → 标注缺失范围。
- **派生字段无请求模块**：市值 = 字段聚合器读缓存（股本 + raw close）现算，不落盘。
- **调整顺序/新增来源/修 bug**：改字段链注册或单个请求模块，不影响其他模块。

## 2. 行情获取工具 `get_quote`

### 2.1 工具输入

| 参数        | 必填 | 默认  | 说明                                                                    |
| ----------- | ---- | ----- | ----------------------------------------------------------------------- |
| code        | ✅   | —    | 带后缀代码（见 1.6）                                                    |
| vars        | —   | close | 返回字段列表，可多选；date 恒保留。可选：open/high/low/close/volume/amount/turnover/outstanding_share/total_market_cap/float_market_cap 等（历史代码曾用 `fields`，作废） |
| adjust      | —   | raw   | 复权方式：raw=除权价 / hfq=后复权 / qfq=前复权                          |
| start_date  | —   | 空    | 日期起点，空 = 最近 10 个交易日                                         |
| end_date    | —   | 空    | 日期终点，空 = 当天系统日期                                             |
| period      | —   | daily | daily/weekly/monthly；周/月线由日线现算不落盘                           |
| export_path | —   | 空    | 空 = 直接返回数据（超长自动导出，见 1.6）；指定 = 导出并返回元信息      |

### 2.2 复权口径与获取逻辑（决策）

- **a. 除权价**：新浪单源直取，高开低收四项**一次请求打包获取**（不单独获取单项），需要什么从缓存读取/下载。来源链见 1.4。
- **b. 后复权价**：**只存收盘价 + 因子还原 OHL，单源无回退（2026-08-09 用户拍板：避免不同源数据混杂，失败就是失败）**：
  - **基本口径 = 现金分红（价格复权），不用红利再投**（2026-08-09 用户拍板：与主流行情工具口径一致、可交叉验证；红利再投口径会污染技术分析并在除息日产生假收益）——**美股例外：统一分红再投**（见下）；
  - A 股/港股：**新浪**（akshare `stock_zh_a_daily`/`stock_hk_daily` adjust=`"hfq"`，全历史实测可用：600519 2001 年至今、00700 2004 年至今、00941 1998 年至今；现金分红口径）；
  - 美股：**iFinD，全程分红再投（2026-08-09 用户拍板：iFinD 单点无现金分红参数，为保证美股 5 年前后一致可比，全程分红再投；不使用 THS_HQ `CPS:7` 现金分红——单点无对应口径会混源）**：
    - 近 5 年：**THS_DS 指标 `close_price`，参数 `107,OC`**（=分红再投，实测 AAPL.O 2021-08-09 至今 1255 行；THS_DS/THS_HQ 序列均限 5 年）；
    - 5 年前：**单点 THS_BD `107,OC` 按交易日历逐日补全**（`THS_Date_Query('212010','mode:1,dateType:0,period:D,dateFormat:0',...)` 纽交所交易日历，仅取交易日，每年约 251 次请求；单点不限 5 年）；
    - **代码后缀用 THS_HQ 探测**（`.O`/`.N`：`THS_HQ('AAPL.O','close','Interval:W','2026-08-01','2026-08-08')` 返回数据即后缀正确，脚本不猜后缀，O 错试 N，都不对报错）；
  - ⚠️ **iFinD 复权口径分市场语义不同（2026-08-09 实测确认）**：
    - **A 股**：THS_DS `107,OC` = 现金分红（与新浪 hfq 一致，600519 五年比值恒定 0.9965）；
    - **美股**：THS_DS `107,OC` = **分红再投**（=THS_HQ `CPS:3` = THS_BD 单点 107，AAPL 2026-08-07 均为 91659.19）；**现金分红 = THS_HQ `CPS:7`**（72500.58，差 26.4%，比值 0.795 且漂移——是不同复权方式，不是基准差）；
    - 单点（THS_BD）只有分红再投口径（用户查过手册确认：无现金分红参数）；
    - **美股与 A/港股跨市场口径不同**（美股分红再投 vs A/港现金分红），由 AI/用户自行注意；
  - ⚠️ **美股 hfq 覆盖边界（上游问题，确认不修复，有就接受）**：某些美股的后复权有效数据可能短于 5 年——COHR.N 实测 5 年窗口 1255 行中仅 867 行有值（**2023-02-23 起**，更早 388 行 NaN 占位）；此类公司更名前无因子、无法还原 OHL/qfq，缺失范围标注即可；
  - **缓存 hfq 文件只存 `date+close` 一列**（不存 OHL），逐日复权因子 `F(t) = hfq_close(t) / raw_close(t)`，需要后复权 OHL 时还原：`hfq_OHL(t) = raw_OHL(t) × F(t)`（raw 同为新浪口径，因子自洽）；
  - **还原精度实测（2026-08-09）**：A 股 600519 全历史 5978 日还原 open/high/low 最大误差 0.035%（hfq 小数位舍入，可忽略）；**港股除权日失真**——新浪直返的 hfq OHL 在除权日本身不可信（2023-12-22 open 用除权前因子 5.6717、close 用除权后因子 5.7771，K 线日内假跳变 ~1.9%；raw 同日无此异常），close 始终可靠；还原方案不会比源直返更差，除权日 OHL 失真在 notes 标注即可；
  - 源失败 = 缓存未更新，提示重试，**不跨源补缺**；重试时自动跳过已完成部分。
- **c. 前复权价**：**本地计算，不缓存**。`前复权(t) = 后复权(t) × (最新不复权收盘 / 最新后复权收盘)`，除权因子取同一交易日（日期对齐），随分红自动更新。
- 请求前复权时：自动获取后复权 + 除权的高开低收到缓存，再从缓存读取并计算前复权。
- 缓存只存字段级 9 个 json（open/high/low/close/close_hfq/volume/amount/total_shares/floating_shares，见 1.5），均为静态历史永不过期；新浪 A 股 outstanding_share 写入 floating_shares.json（source=sina，A 股流通股本口径）；turnover 换手率不缓存、派生算（volume ÷ 流通股本）。
- 新浪行情且请求周/月线：本地把日线聚合成周/月线（规则见 1.5），返回 `notes` 提示"新浪接口仅提供日线，weekly/monthly 由日线本地聚合"。

### 2.3 字段获取方式（决策）

| 字段 | 获取方式（决策） |
| ---- | ---------------- |
| open/high/low/close（raw） | 核心组一次打包（新浪单源） |
| volume / amount | 随核心组打包（美股 amount=仅 iFinD，A/港=新浪 raw 宽写列） |
| total_market_cap / float_market_cap | 计算法（股本 × 不复权收盘，见 2.4），现算不落盘 |

各字段 × 各数据源的**客观可得性**见 `字段与数据源支持情况.md` §2。

### 2.4 市值（计算法，2026-08-08 定稿）

- **定案：市值 = 股本 × 当日不复权收盘价（逐日对齐 K 线），现算不落盘；不做 iFinD market_value 直接获取。**
- 理由：已有股本和行情价格即可自算，无需 iFinD 提取；估算值统一标注"总市值/流通市值为估算值（股本 × 不复权收盘价）"。
- **股本来源链（2026-08-09 唯一源拍板）**：A/B/北交所总股本 iFinD（§7 方案）、流通股本新浪 outstanding_share；港美股总股本+流通股本 iFinD（§7 方案）。yfinance 不接入（唯一源原则）。
- **流通市值**：A/B 股用新浪 outstanding_share；港美股用 iFinD floating_shares（近 5 年序列 + 早期单点月频）；yfinance 无 float 历史序列（仅当前值可作参考）。
- **不从财报提取季度股本反推历史股本**（2026-08-08 决策：复杂且易错，后面确有需要再处理；A 股资产负债表 SHARE_CAPITAL 数据存在但暂不接入）。
- 股本低频变化（回购/增发/解禁/送转才变），月频采样足够；缓存带 source 元数据，不跨源填充（防 yfinance 扣库存股与 iFinD 口径差异）。
- 币种：**原始币种**（股本 × 原币收盘；A 股人民币/港股港元/美股美元），不做汇率换算。

### 2.5 返回结构

- 默认（export_path 空）：`{ok, market, code, start, end, adjust, period, vars, source, notes, rows}`——rows 为数据本体（date 恒在，其余按 vars 与市值列）；**超阈值自动导出**（见 1.6）时返回 `{ok, auto_exported: true, path, total_items, date_range, source, notes}`。
- 指定 export_path：`{ok, path, total_items, date_range, source, notes}`，不含 rows。
- `notes`：非空 = 降级/聚合/估算说明（如"新浪接口仅提供日线，weekly/monthly 由日线本地聚合"、"市值为估算值"、"已自动导出到 {path}"、"后复权仅从 2023-02-23 起"）；AI 必须感知。
- `source`：sina / iFinD / eastmoney——告知实际取数路径，AI 据此判断字段完整性。

## 3. 财报获取工具 `get_financial_statements`

### 3.1 定位与形状（决策）

三市场原始三表（利润表/资产负债表/现金流量表）获取。A/B/北交所和港股三表使用东财；美股三表使用东财，公共提交元信息使用 SEC submissions。每类信息固定单源，不补查第二源、不做来源回退。

**统一长表形状、中文优先展示**：

```json
rows: [{
  "report_date", "statement", "item_name", "amount", "source",
  "value_basis", "version_id", "is_latest", "version_count", "has_revisions",
  "first_seen_at", "source_update_date", "change_summary", "report_metadata"
}, ...]
```

- 缓存始终保留上游原始代码/名称；返回与 CSV 导出中文优先。有经实际报表金额与正式名称核验的 A 股映射时只返回中文，未映射则返回原名；港股/美股沿用上游中文。
- 不做跨市场统一命名。中文映射只解决 A 股英文代码的可读与精确查询，不把港美科目强制改成 A 股口径。
- 只保存上游原始金额，丢弃 `_YOY/_QOQ/_MOM` 等比例或增长率列；所有 null 科目均保存，顶层 `item_catalog` 维护跨版本完整科目目录。
- 数值单位：**原始货币（元）**——人民币/港元/美元原币数值，不做单位换算（2026-08-08 定）；返回/导出时在元信息中标注币种。

### 3.2 参数

| 参数 | 语义 |
| ---- | ---- |
| `code` | 必填，强制市场后缀 |
| `amount_basis` | 必填：`cumulative`=累计原值；`single`=最新累计版本现算当期金额；不提供 `both` |
| `statements` | `balance/income/cash_flow` 非空子集；不传返回三表。只控制返回，刷新始终同时获取三表 |
| `start_date/end_date` | 按原始报告截止日筛选，`YYYY-MM-DD`，含边界；都不传为全部历史；可只传一端 |
| `include_versions` | 默认 false，每个报告期只返回最新版本；true 时累计口径返回全部历史版本 |
| `force_refresh` | 忽略 30 天新鲜期并强制联网刷新 |
| `export_path` | 指定则导出 CSV；未指定且结果超过 200 行时自动导出到 `cache/_auto_export/` |
| `items` | 可选中文科目名列表；精确匹配优先，失败后按字符顺序做关键词匹配。单候选直接返回并提示；多候选进入 failed_items，附各候选最近非空金额；无候选不计算衍生指标 |
| `report_types` | 可选 `annual/semiannual/q1/q3` 列表，可组合；与 single 组合分别表示 Q4/Q2/Q1/Q3 单季 |

`get_data_catalog(code, statements)` 仅从现有四文件缓存汇总该公司对应报表所有报告期、所有版本中至少有一次非空金额的科目，并返回最早/最晚有效报告日；不联网、不额外维护目录文件，无缓存时提示先调用 `get_financial_statements`。

### 3.3 缓存、刷新与版本

每家公司固定四个 JSON：

```text
cache/{code}/financial_statements/
  metadata.json
  balance.json
  income.json
  cash_flow.json
```

- 三张表文件按 `report_date` 分组，每期含 `current_version_id/versions[]`；版本保存原始报告级元信息和完整 `items[]`。
- `metadata.json` 保存公司级来源配置；美股另存 CIK、SEC 财报提交索引和 `sec_history_complete`。首次读取 SEC 历史分片，后续只取 recent 并按 accession 合并。
- 同报告期的“全部原始科目金额 + 有业务语义的报告元信息”计算 SHA-256；内容变化生成新版本，相同内容只更新 `last_seen_at`。`change_summary` 记录新增、删除、变化科目代码及变化元信息键。
- 四文件共用 `update_batch_id`。三表与公共元信息先写临时文件，再按同一批次替换；任一上游请求或结构校验失败均不提交新批次。
- 结构损坏（空响应、缺关键列、报告日不可解析、重复科目值冲突、金额无法 JSON 化）阻断整批更新；不猜测跨市场会计勾稽关系，不自行修正上游值。

### 3.4 累计与单期口径

- 资产负债表始终返回时点值 `point_in_time`，不参与相减。
- `cumulative` 返回缓存中的原始累计金额；`include_versions=true` 可查看同报告期全部历史版本。
- `single` 只使用各报告期当前最新累计版本，在同公司、来源、财年区间、币种、准则且报告期间连续时相减；使用上游 `REPORT/REPORT_DATE_NAME/REPORT_TYPE/START_DATE` 判断期间，不按月份猜财季。筛选日期前先在完整缓存上派生，因此单独查询 Q2 仍可使用 Q1。
- `single` 不返回 EPS、每股股息、加权平均股数等非加总科目。这些科目只能从 `cumulative` 获取；额外加工由调用方自行处理，工具不提供支持。
- `include_versions=true` 与 `single` 同时使用时仍只生成最新视图的单期金额，并返回 `derived_from_version_ids`；工具不生成历史单期版本。

### 3.5 已实现与真实验证（2026-08-09）

- `600519.SH`：资产/利润/现金流缓存 102/102/98 期，科目目录 152/95/120；派生比例列 0，null 与公告日、更新日、币种、审计意见均保留。
- `00700.HK`：三表 92/95/95 期，科目目录 71/32/60；summary 中期间起点、报告类型、财年截止、币种、准则已绑定到各报告版本。
- `AAPL.US`：资产/利润/现金流 102/105/105 期；利润和现金流只保存 26 年报 + 26 个 Q1 + 53 个累计季报，不保存 Q2/Q3/Q4 单季记录；SEC CIK `0000320193`，财报相关 submissions 132 条。强制二次刷新未重复下载历史分片，相同三表内容未生成重复版本。
- MCP 工具发现、MCP 真实调用、CLI stdin 批量入口、30 天缓存复用、强制刷新、stale 回退、显式/自动 CSV 导出均已实跑。

### 3.6 数据可得性

各市场 × 各源的客观可得性见 `字段与数据源支持情况.md` §3（东财 datacenter 域稳定，2026-08 实测不受行情域限流影响）。

## 4. 衍生数据获取工具 `get_financial_ratios`

### 4.1 定位（决策）

财务衍生指标，**原始返回、东财列名原样**（如 ROEJQ=加权ROE、ROEKCJQ=扣非ROE、MLR=毛利率、EPSJB=基本EPS、BPS=每股净资产）。命名依据：行业标准把原始三表叫 Statements、衍生比率叫 Ratios，不叫 indicators（易误解为技术指标 MACD/RSI）。数据源=东财。

### 4.2 参数

`code`（必填）、`periods[]`（年份列表，空=全部）。

### 4.3 数据可得性

客观可得性见 `字段与数据源支持情况.md` §4（港股仅最新 TTM ⚠️）。

## 5. 公司信息获取工具 `get_company_profile`

### 5.1 定位（决策，2026-08-09 讨论定稿）

公司概况/IPO/分红/盈利预测/股东（首版五类），**按 section 分缓存文件**（profile.json / ipo.json / dividends.json / forecast.json / holders.json），`sections` 可选（空=全部）。**主营构成归属财报工具**（get_financial_statements 扩展：利润表科目的业务线细分，同报告期同金额体系可交叉验证），当前不实现、之后完善。回购是独立全市场接口（stock_repurchase_em 全市场 5455 条按代码过滤），不是分红子项，首版不纳入。

### 5.2 多源合并策略（决策 A，2026-08-09 用户拍板）

**主源优先，缺的信息从其他渠道补充**——与行情"每字段唯一数据源"同原则：每个字段只有一个来源值，主源没有该字段时才从补充源取；同一字段绝不出现两个来源的值。

**回退 vs 补全（2026-08-09 用户澄清）**：**无失败回退**——所有 section 主源失败即结构化报错（可重试），不做第二来源顶替（A 股尤其如此，降低复杂度）；**补充源只做字段补全**——主源成功但缺某字段时，从补充源取该字段，不参与失败回退。

| section | 市场 | 主源 | 补充源（仅字段补全，不参与失败回退） |
|---|---|---|---|
| profile | A股 | 巨潮 stock_profile_cninfo（26 字段）——**单一来源** | — |
| | 港股 | 东财 stock_hk_company_profile_em（17 字段）**+ stock_hk_security_profile_em（14 字段）合并字段并集**（ISIN/每手股数/沪深港通标识在证券接口，两接口字段互补无重叠） | — |
| | 美股 | ❌ 无结构化 → 返回"用公告 MCP 查 SEC"提示 | — |
| ipo | A股 | 巨潮 stock_ipo_summary_cninfo（宽表 15 字段） | 新浪 stock_ipo_info（key-value 17 项） |
| | 港股/美股 | ❌ → 公告提示 | — |
| dividends | A股 | 东财 stock_fhps_detail_em（18期×19字段最全，含股息率/EPS/总股本） | 同花顺 stock_fhps_detail_ths（独有股利支付率/税前分红率/不分配记录）；巨潮（年度/中期类型）；新浪（精简）——仅取主源缺失字段 |
| | 港股 | 东财 stock_hk_dividend_payout_em（27期） | — |
| | 美股 | ❌ → 公告提示 | — |
| forecast | A股 | 东财 stock_profit_forecast_em（一致预期+评级分布，全市场拉取后按代码筛） | 同花顺 stock_profit_forecast_ths（min/max/行业均值） |
| | 港股 | 经济通 stock_hk_profit_forecast_et（分券商明细） | — |
| | 美股 | ❌ | — |
| holders | A股 | 新浪 stock_main_stock_holder（历史序列，多期叠加） | 东财 stock_gdfx_free_top_10_em（实测 KeyError 'sdltgd'，格式待验证） |
| | 港股/美股 | ❌ → 公告提示 | — |

### 5.3 参数

`code`（必填）、`sections[]`（profile/ipo/dividends/forecast/holders，空=全部）、`force_refresh`（默认 false，忽略 30 天新鲜期强制联网刷新）。

### 5.4 缓存与刷新

- 每个 section 一个 JSON 缓存文件，**快照性质**（meta 含 updated_at/source/market），无行情式日期段覆盖概念。
- **刷新节奏与财报一致（2026-08-09 用户拍板）**：**30 天新鲜期**——缓存 30 天内直接返回，超过 30 天自动联网刷新（分红/预测/股东变化节奏与财报披露一致，分红本身就在财报里披露）；`force_refresh=true` 忽略新鲜期强制刷新。不引入分层 TTL。
- 返回：`{ok, code, sections: {...}, notes}`——每个 section 返回其对象/列表；美股 section 返回 `{"available": false, "reason": "美股无结构化公司信息，请用公告 MCP 查询 SEC 提交"}`（不是错误）。
- 港股 profile 返回两接口字段并集（公司 17 + 证券 14，去重）。

### 5.5 数据可得性

客观可得性见 `字段与数据源支持情况.md` §5。

### 5.6 待办

- [ ] **美股 iFinD 5 年前回填覆盖最新数据点（真实 bug，测试红）**：`_fetch_us_daily` 把 THS_DS 近 5 年 + THS_BD 补 5 年前的 `fresh` 整体写入，回填单点（如 2021-08-06）排在 fresh 末尾，`_write_field` 后写覆盖丢掉了 THS_DS 返回的最新交易日（2021-08-09）。测试 `test_backfill_uses_trade_days` 断言 `items[-1] == 2021-08-09` 失败。修法：合并时按日期升序排序/去重，保证最新交易日保留。2026-08-12 记录（低频路径：美股成交额 5 年前回填）
- [ ] 主营构成（stock_zygc_em 行业/产品/地区三维度）→ 财报工具扩展（statements 加 zygc），当前不实现
- [x] 刷新策略定稿：30 天新鲜期 + force_refresh（与财报一致，2026-08-09）
- [ ] 东财股东接口 stock_gdfx_free_top_10_em 格式验证（KeyError 'sdltgd' 待解）

## 6. 明确不做（边界）

- ❌ 不做汇总文档工具（用户自己写脚本汇总）
- ❌ 不做跨市场统一命名（A股仅做经核验的中文展示映射）
- ❌ 不做实时行情（只做历史日/周/月线）
- ❌ 不做分钟级数据
- ❌ 不做汇率换算（原币返回）
- ❌ 不记录缓存失败状态（失败即提示重试）
- ❌ 不从财报提取季度股本反推历史股本（复杂易错，后需再处理）
- ❌ EODHD qfq→hfq 因子转换恢复（2026-08-09 停用：后复权单源化；转换方法保留为知识——`hfq(t)=qfq(t)×factor`，300476.SHE 曾实测误差 ≤0.0006%）

## 7. 股本获取方案（2026-08-08 讨论定稿，未编码）

### 7.1 背景与决策

- 股本用途：总市值/流通市值估算（股本 × 不复权收盘价，逐日对齐）。**股本低频变化**（回购/增发/解禁/送转才变）→ 月频采样足够估算市值。
- 免费渠道：A/B 股东财季度 + 新浪逐日（全历史）；港美股 yfinance（2015 底起 10 年，**当前不接入**）；**2015 年前无免费渠道**。
- **决策：股本尽可能使用 iFinD（唯一全市场全历史官方来源，用户拍板）；A 股流通股本=新浪 outstanding_share（唯一源，2026-08-09 拍板）；避免使用财报提取股本（数据太杂，后期确有需要再处理）**。

### 7.2 获取方案（序列 + 单点月频混合）

```
输入：code + start + end（目标日期范围）
① 序列（THS_DS）：窗口从【今天】往前 5 年（服务端视角，不是从 end 倒推）
   请求 [今天-5年, min(end,今天)]，返回多少算多少，得序列实际起点 ds_start
② 单点（THS_BD 月频）：范围 [start, ds_start)，采样点=每月【日历月末日】（1/31、2/28…）
   获取顺序从 ds_start 往 start 倒序（先近后远）→ 中断只丢最老数据，不会中间断
③ 合并：近 5 年逐日 + 早期月频点，排序去重；部分失败不丢弃（{ok, data, failed, notes, elapsed}）
```

**关键决策**：

1. 近 5 年窗口以今天为锚：THS_DS/HistoryQuotes 限制是"最近 5 年"（服务端视角），不是任意窗口。
2. **日历月末日方案（无需交易日历）**：THS_BD 非交易日自动返回最近交易日值 → 日历月末日请求 = 该月最后一个交易日的股本（月末当天是交易日→当天值；不是→前一个交易日仍在本月；长假横跨月末→节前最后交易日值，语义正确）。与"交易日历月末"结果一致，省掉日历调用。
3. 月频采样理由：股本低频变化 + 市值估算用前置填充（merge_asof backward），月频足够；单点串行慢且耗配额，2010~2021 月频 ≈ 139 次请求 ≈ 1-2 分钟（全量逐日 2900 次 ≈ 25 分钟）。
4. 获取顺序先近后远：序列完整拿近 5 年，单点倒序补早期，限额/失败只影响最老数据。
5. **多源冲突规避**：同一字段同一公司只走单一来源链（字段为中心）；月频点与日频点并存无冲突（同源同口径，前置填充取最近点）；跨源口径差异需防（yfinance Ordinary Shares 扣库存股 vs iFinD total_shares 可能含库存股，0700.HK 差 87M）→ 缓存带 source 元数据，不跨源填充。

### 7.3 iFinD 股本接口能力

接口/参数/限制的客观记录见 `字段与数据源支持情况.md` §7；已实测验证：AAPL 2010 至今 THS_BD 月频 139/139 点成功且与 THS_DS 无缝衔接；0700.HK 2004 早期抽样成功；600519 2015 全量 2818 交易日交叉验证一致。

## 8. 历史关键决策（时间线，防重复讨论）

| 日期 | 决策 | 状态 |
| ---- | ---- | ---- |
| 08-07 | 工具面 4 工具（quote/statements/ratios/profile） | ⚠️ 08-09 增加轻量 get_data_catalog，现为 5 工具 |
| 08-07 | 行情复权请求 qfq/hfq/raw 三份缓存 | ❌ 废弃（08-08：只缓存 raw+hfq，前复权本地算） |
| 08-07 | iFinD 行情第一来源 | ❌ 废弃（08-08：SDK bug+仅 5 年，降级为仅股本） |
| 08-07 | 市值 iFinD 直接获取 | ❌ 废弃（08-08：计算法，股本×不复权收盘） |
| 08-07 | 统一"下载到缓存不返回数据本体" | ⚠️ 部分废弃（08-08：默认返回+超长自动导出） |
| 08-08 | 腾讯量额剔除（单位不稳定），仅 OHLC 价格回退 | ✅ 有效（08-09 腾讯整体退出行情链） |
| 08-08 | 字段为中心来源链、EODHD 入价格链 | ⚠️ 部分废弃（08-09 EODHD 退出） |
| 08-08 | 市值计算法、不从财报反推股本 | ✅ 有效 |
| 08-08 | 默认返回+超长自动导出（200 行）+指定导出 CSV | ✅ 有效 |
| 08-08 | EODHD qfq→hfq 因子转换恢复 | ❌ 停用（08-09 后复权单源化） |
| 08-09 | 行情单源化：raw=新浪（A/港/美/北交所）、hfq=A/港新浪+美股 iFinD、单源无回退 | ✅ 有效（本表核心） |
| 08-09 | 后复权基本口径=现金分红；**美股例外=全程分红再投**（单点无现金分红参数） | ✅ 有效 |
| 08-09 | hfq 只存 close + 因子还原 OHL | ✅ 有效 |
| 08-09 | 美股后缀 THS_HQ 探测（.O/.N）；纽交所交易日历 THS_Date_Query 212010 | ✅ 有效 |
| 08-09 | 美股 hfq 覆盖边界（COHR 等）：上游问题不修复，有就接受 | ✅ 有效 |
| 08-09 | **美股 amount = iFinD 单源**（用户提供官方公式 `amt,OC`：近 5 年序列 + 更早交易日历单点逐日补全；单点非交易日返回空值）；东财美股 amount 废弃（限流从未实测成功） | ✅ 有效 |
| 08-09 | **缓存改字段级独立 json**（用户拍板）：9 个文件 items `[{date,value,source}]` 每字段同日期仅一条（source 为追踪标记）；请求模块"拉到什么写什么"（一次请求更新多个字段 json）；**每字段唯一数据源、不做来源回退**（A/北交所 floating_shares=新浪、港美股=ifind 等）；派生全现算（hfq OHL 因子还原/qfq/市值/换手率/周月） | ✅ 有效 |
| 08-09 | **覆盖判定探测验证**（用户拍板，无交易日历）：补拉后返回末日 < end 且市场已收盘 → 写 meta `verified_until`+`verified_at`（秒级）；周末探测延伸覆盖整个周末（周六查过数据到周五 → 周六/周日零请求）；盘中不写（防当日数据被误缓存）；节假日靠探测机制覆盖（周五休市第一次补拉空 → verified → 大规模分析零重复请求）；收盘时间表：A/BJ 15:00、HK 16:00、US 美东 16:00（含夏令时换算） | ✅ 有效 |
| 08-09 | **探测状态机**（用户拍板）：meta 记录 `last_probe {state: intraday\|closed, date}`——盘中探测只记 intraday（同天盘中续探零请求，本次盘后自动失效补拉当天数据）、盘后探测写 verified_until+verified_at；跨天自然失效；盘中接口真实行为待周内实测 | ✅ 有效 |
| 08-09 | EODHD 付费版评估取消；腾讯退出行情链 | ✅ 有效 |

## 9. 待办

- [ ] **get_quote 未完成日线**（2026-08-17 下游反馈，待拍板）：日期骨架用各字段并集，会输出「今天」且 close 为空或抄前一日的行。现象与复测见 `docs/下游反馈-get_quote未完成日线-2026-08-17.md`。与「不做实时行情」冲突。
- [ ] **接口返回格式表**：客观能力已实测填充（见数据源文档 §8）；东财美股 `stock_us_hist` 列名待限流恢复后复测（东财已退出行情链，降为知识补全，非必需）
- [x] 北交所行情：新浪 raw+hfq 均支持（2026-08-09 实测 bj920002，2024-05-30 上市至今 533 行，因子 ~1.97 送转有效）✅
- [x] 北交所股本 iFinD 代码格式：`920002.BJ` THS_BD 实测可用（2026-08-08）✅
- [x] EODHD qfq→hfq 因子转换端到端验证：300476.SHE 28 重叠日误差 ≤0.0006%（2026-08-08）✅（2026-08-09 起停用）
- [x] EODHD 付费版评估——取消（2026-08-09：EODHD 已退出行情链）
- [x] 市值估算端到端验证：AAPL 2010 至今 139 月频点全成功、0700.HK 2004 起抽样（2026-08-08）✅
- [x] MCP 注册（Hermes config + 能力库）与 export.py 双包导出（2026-08-09 完成，一次导出两个 MCP 到用户版同目录）✅
- [x] hfq 因子还原实现与验证：600519 还原误差 0.035% 已实测 ✅；**美股 iFinD close 还原 OHL 拆股日行为待验证**（AAPL 拆股日，挂起）
- [x] yfinance 后复权：实测不支持（只有前复权 Adj Close + 事件列），美股 hfq 维持 iFinD 唯一源（2026-08-09 确认）✅
- [x] `get_financial_statements`：四文件缓存、版本保留、三表批次、A/HK/US+SEC provider、累计/单期、MCP/CLI、真实三市场验证（2026-08-09）✅

> 已定稿（2026-08-08）：导出文件格式 = CSV；财报数值单位 = 原始货币（元）；市值币种 = 原始币种；超长自动导出阈值 = 200 行。
