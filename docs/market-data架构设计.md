# market-data MCP 架构设计

> 2026-08 定稿。本文档基于此前多轮讨论与上游接口实测重新组织，是 market-data MCP 的架构权威依据，实现按此执行。
> 状态标注：✅ 已实测　⚠️ 有限（降级/仅当前值）　❌ 不可用　❓ 尚未确定
> 对应仓库：Ashare-announcements-MCP（与公告 MCP 同仓库，独立包 `market_data_mcp`）。

## 1. 整体定位和说明

### 1.1 功能与定位

三市场（A 股/B 股/港股/美股）**结构化数据**获取工具，与公告阅读 MCP（文档层）并列互补：

| | 公告 MCP（已有） | market-data MCP（本设计） |
|---|---|---|
| 数据类型 | 公告/提交文档（非结构化） | 行情/财报/指标/概况（结构化） |
| 数据源 | 东财公告接口 + SEC EDGAR | iFinD SDK → AKShare 东财 → AKShare 新浪 → 腾讯(仅价格) |
| 消费方式 | 查询/阅读/搜索 | 下载到本地缓存 + 按需返回/导出 |

个人投资工具。不做汇总文档工具（汇总由用户自己的脚本做）、不做实时行情、不做分钟级数据、不做跨市场科目映射。

### 1.2 工具面（4 工具）

| 工具 | 类型 | 参数 | 缓存文件 |
|---|---|---|---|
| `get_quote` | 行情 | code, vars, adjust, start_date, end_date, export_path | quote_daily_raw / quote_daily_hfq / market_cap |
| `get_financial_statements` | 财报 | code, periods[], statements[] | financial_income / balance / cash_flow |
| `get_financial_ratios` | 衍生指标 | code, periods[] | ratios |
| `get_company_profile` | 公司信息 | code, sections[] | profile |

### 1.3 市场与字段范围

- **市场**：A 股（沪/深/创业板/科创板/北交所）、B 股（沪B/深B）、港股、美股正股（不做 ETF）。
- **行情字段**：除权价（OHLC）、后复权价（OHLC）、前复权价（OHLC，计算）、成交量、成交额、流通市值、总市值。
- **非行情**：财报三表、财务指标、公司概况/分红/盈利预测。

### 1.4 数据源体系

**优先级（行情）**：iFinD Python SDK（第一）→ AKShare 东财 → AKShare 新浪 → 腾讯（仅 OHLC 价格回退）。

- **iFinD**：用户有正式账号（config 填账号密码，支持多账号，有则用、无则跳过）。国内服务，清代理直连。
- **东财**（AKShare）：三市场三表/指标/概况的 datacenter 域稳定；push2his 行情域 2026-08 持续限流（属预期，新浪回退是当前常态路径）。
- **新浪**（AKShare）：A 股全能力；港股/美股无 start/end 参数（拉全量本地过滤）；美股无后复权、无成交额。
- **腾讯**：volume/amount 单位不统一（主板/创业板按手、科创板按股，实测），**剔除出量额回退**，仅保留 OHLC 价格链。
- **yfinance**：仅用于港股/美股历史股本估算（回退场景），必须走代理 127.0.0.1:17891（直连被 Yahoo 限流），上限约 10 年。

**核心原则：字段为中心，来源可插拔。** 每个字段有自己的来源优先级链（如 close: iFinD→东财→新浪→腾讯），链上任一源拿到即填入缓存，全链失败才标注缺失。数据源是可替换实现（后续可加第 4/5/6 源，iFinD 本身也可作为独立源接入）。**不以数据源为基本单位，以字段为基本单位。**

### 1.5 缓存与更新策略

- 缓存存 `cache/{代码}/` 下，JSON 格式 `{meta, items}`：meta 含 code/market/data_type/adjust/source/updated_at/date_range/shape；items 为数据本体。
- **首次全量、之后增量**：行情拉 last_date 之后新交易日；财报拉新报告期。
- **失败 = 缓存未更新**：直接返回"缓存获取失败，请重试"，不记录失败过程；重试时自动跳过已完成部分。
- **无论请求什么复权类型，都会自动更新除权和后复权缓存**；前复权现算不落盘。
- 周/月线由日线现算，不落盘（聚合规则：open=周期首日、high=周期最高、low=周期最低、close=周期末日、volume/amount=周期累计；weekly 用 W-FRI、monthly 用 ME）。

### 1.6 输入输出规范

**股票代码（强制后缀，程序按后缀路由市场）**

| 市场 | 格式 | 示例 | iFinD 转换 | AKShare 转换 |
|---|---|---|---|---|
| 沪（含沪B/科创板） | 6 位 + .SH | 600519.SH / 900901.SH / 688981.SH | 原样 | 裸码 |
| 深（含深B/创业板） | 6 位 + .SZ | 000001.SZ / 200725.SZ / 300476.SZ | 原样 | 裸码 |
| 北交所 | 6 位 + .BJ | 920002.BJ | 待确认 ❓ | 裸码 |
| 港股 | 5 位 + .HK | 00700.HK | 0700.HK（4 位带前导零） | 00700 |
| 美股 | 字母 + .US | AAPL.US | AAPL.O（.O 后缀） | AAPL |

- 后缀大小写不敏感（程序统一处理）。
- **同一个后缀走同一条通道**（如 900901.SH 与 600519.SH 同为沪通道，B 股已实测与 A 股完全同渠道）；**禁止按数字位数/开头猜测市场**（沪B 900901 曾因"6 开头=沪"逻辑误归深——脚本不猜测，AI 负责灵活）。
- 若数据源层提示代码不对，直接报错并给出清晰说明（说明该后缀对应哪个市场、当前源接受的格式），不静默降级重试。

**日期**：`YYYY-MM-DD`。日期起点默认空 = 最近 10 个交易日；日期终点默认空 = 当天系统日期。

**导出路径**：默认空 = 数据直接在工具调用返回；指定路径 = 导出数据到对应位置，返回值不再包含具体内容（只返回元信息：路径/条数/范围）。导出文件格式尚未确定 ❓（实现时定）。

**币种**：所有请求的行情、成交额均为**原币**（A 股人民币/港股港元/美股美元），工具不换汇率；需要其他币种由调用方自行换算。市值由 iFinD 直接提供，实测返回 USD（见 2.7），币种口径待确认 ❓。

### 1.7 如何新增

**新增市场**：① 后缀路由表加一行（后缀→渠道）；② 各数据源代码格式转换（iFinD/AKShare 差异表）；③ 字段表逐列补该市场的来源链；④ 按探测纪律实测各源（一次一小点、带超时、先查接口签名）；⑤ 补测试。⚠️ 2026-08 现状：北交所行情渠道未单独实测。

**新增字段/变量**：在字段表定义字段名 + 来源链（或派生计算式，如市值=股本×不复权收盘）→ 链上任一环节成功即有值 → 全链失败标注缺失 → 只动该字段，不影响其他字段。

**新增数据源**：实现可插拔 provider（含代码格式转换、代理策略、复权/频率能力声明）→ 接入各字段链 → 探测验证数据一致性（与现有源逐日对比）→ 补测试。

**维护**：接口签名以 `inspect.signature` 实测为准（akshare 版本升级后复测）；复权/频率支持矩阵见 2.2；代理差异（国内源清代理直连、yfinance 走代理）见 1.4；NaN 清理必须显式逐值判断（float NaN / pd.NaT 转 None，`df.where(pd.notnull())` 对 float NaN 不生效）。

## 2. 行情获取工具 `get_quote`

### 2.1 工具输入

| 参数 | 必填 | 默认 | 说明 |
|---|---|---|---|
| code | ✅ | — | 带后缀代码（见 1.6） |
| vars | — | close | 变量：open/high/low/close 4 个价格变量，可多选，默认收盘价；date 恒保留 |
| adjust | — | raw | 复权方式：raw=除权价 / hfq=后复权 / qfq=前复权 |
| start_date | — | 空 | 日期起点，空 = 最近 10 个交易日 |
| end_date | — | 空 | 日期终点，空 = 当天系统日期 |
| export_path | — | 空 | 空 = 直接返回数据；指定 = 导出到该路径，返回元信息 |

可选参数（沿用此前讨论定稿）：`period`（daily/weekly/monthly，默认 daily，周/月线由日线现算不落盘）。

### 2.2 复权口径与获取逻辑

- **a. 除权价**：高开低收四项**一次请求打包获取**（不单独获取单项），需要什么从缓存读取/下载。数据源：A/B 股 iFinD→东财→新浪；港股 iFinD→东财→新浪；美股 iFinD→东财→新浪。
- **b. 后复权价**：**直接使用数据源返回的数据，不做任何处理**（不换汇率、不做任何改动）。iFinD CPS:3 / 东财 hfq / 新浪 hfq 均为提供商原值。
- **c. 前复权价**：**计算得出，不缓存**。`前复权(t) = 后复权(t) × (最新不复权收盘 / 最新后复权收盘)`，除权因子取同一交易日（日期对齐），随分红自动更新。
- 请求前复权收盘价时：自动获取后复权 + 除权的高开低收到缓存，再从缓存读取并计算前复权。
- 缓存只存不复权（raw）+ 后复权（hfq）两个文件，均为静态历史永不过期。

**复权/频率能力矩阵（akshare 1.18.83 + iFinD 实测）**

| 接口 | 源 | raw | hfq | qfq | weekly/monthly | start/end |
|---|---|---|---|---|---|---|
| iFinD THS_HistoryQuotes | iFinD | ✅ CPS:1 | ✅ CPS:3 | ✅ CPS:2 | ❓ | ✅ |
| stock_zh_a_hist / hk_hist / us_hist | 东财 | ✅ | ✅ | ✅ | ✅ 原生 | ✅ |
| stock_zh_a_daily | 新浪(A股) | ✅ | ✅ | ✅ | ❌ 无参数 | ✅ |
| stock_hk_daily | 新浪(港股) | ✅ | ⚠️ 记录矛盾（一处记无 hfq、一处记源码有 hfq 分支），待复测 | ✅ | ❌ 无参数 | ❌ 拉全量过滤 |
| stock_us_daily | 新浪(美股) | ✅ | ❌ 实测 TypeError | ✅ | ❌ 无参数 | ❌ 拉全量过滤 |

新浪回退且请求周/月线：本地把日线聚合成周/月线（规则见 1.5），返回 `notes` 提示"新浪接口仅提供日线，weekly/monthly 由日线本地聚合"。

### 2.3 行情字段与可得性

| 字段 | 含义 | 获取方式 | A股/B股 | 港股 | 美股 |
|---|---|---|---|---|---|
| open/high/low/close | 除权价 4 项 | 核心组一次打包 | iFinD✅ 东财✅ 新浪✅ | iFinD✅ 东财✅ 新浪✅ | iFinD✅ 东财✅ 新浪✅ |
| volume | 成交量 | 核心组打包 | iFinD✅ 东财✅ 新浪✅ | iFinD✅ 东财✅ 新浪✅ | iFinD✅ 东财✅ 新浪✅ |
| amount | 成交额（原币） | 核心组打包 | iFinD✅ 东财✅ 新浪✅ | iFinD✅ 东财✅ 新浪✅ | iFinD⚠️ HistoryQuotes 恒 None，THS_BD 可单点（批量序列方案待定）；东财✅；新浪❌ 无 amount |
| float_market_cap | 流通市值 | 见 2.4 | iFinD❓ 新浪实时⚠️当前值 | iFinD❓ 新浪实时⚠️当前值 | iFinD❓ 新浪实时⚠️当前值 |
| total_market_cap | 总市值 | 见 2.4 | iFinD✅ THS_DS(USD) 东财快照⚠️ 新浪实时⚠️ | iFinD✅ THS_DS(USD) 新浪实时⚠️ | iFinD✅ THS_DS(USD) 新浪实时⚠️ |

### 2.4 市值（第一批核心字段，与收盘价同级）

- **总市值**：iFinD `THS_DS market_value` 直接提供（三市场实测可用，返回 USD；美股 `ths_market_value_stock` 返回 0 不可用，但 THS_DS 可用）。历史序列能力待实现时确认 ❓。
- **流通市值**：iFinD 字段未探测 ❓；AKShare 无历史市值接口（乐咕已移除）。
- **回退估算**（仅当 iFinD 不可用）：`市值 = 股本 × 当日不复权收盘价`（逐日对齐 K 线，非当前快照值）：
  - A 股总股本：东财资产负债表 `SHARE_CAPITAL`（报告期序列，季度级，merge_asof 向后匹配）✅
  - A 股流通股本：新浪日线 `outstanding_share` 列（每日）✅
  - 港股/美股股本：yfinance 资产负债表 `Ordinary Shares Number`（年报多期约 5 年）+ `get_shares_full`（约 10 年事件点）；⚠️ 2015 年之前不可得；港股需注意库存股（Ordinary = Share Issued − Treasury）
- 估算值标注"总市值/流通市值为估算值（股本 × 不复权收盘价）"；新浪单股实时接口（hq.sinajs.cn）有当前总市值/总股本/流通股本，**仅当前值**，只作"最新市值"参考，不作历史对齐。

### 2.5 返回结构

- 默认（export_path 空）：`{ok, market, code, start, end, adjust, period, vars, source, notes, rows}`——rows 为数据本体（date 恒在，其余按 vars 与市值列）。
- 指定 export_path：`{ok, path, total_items, date_range, source, notes}`，不含 rows。
- `notes`：非空 = 降级/聚合/估算说明（如"新浪美股接口不支持后复权，已返回前复权数据"、"新浪接口仅提供日线，weekly/monthly 由日线本地聚合"、"市值为估算值"）；AI 必须感知。
- `source`：iFinD / eastmoney / sina——告知实际取数路径，AI 据此判断字段完整性（东财路径有 amplitude/quote_change/change/turnover，新浪路径没有）。

## 3. 财报获取工具 `get_financial_statements`

### 3.1 定位

三市场原始三表（利润表/资产负债表/现金流量表）获取。**统一形状、不改科目名**：

```json
items: [{"report_date", "item_name", "amount", "source"}, ...]
```

- 科目名原样保留：A 股宽表内部转长表时科目名不翻译；港股/美股返回什么科目就叫什么科目。
- **不做跨市场科目映射**（映射=自己猜=iFinD 的坑；跨市场比较由 AI/用户脚本处理）。
- 数值单位（元/千元/百万元）是否标注、如何标注：**尚未确定 ❓**（实现时结合具体数据源返回再定）。

### 3.2 参数

`code`（必填）、`periods[]`（报告期年份列表，匹配该年所有季度，空=全部）、`statements[]`（income/balance/cash_flow，空=三表全取）。

### 3.3 数据可得性

| 市场 | 数据源 | 形态 | 状态 |
|---|---|---|---|
| A 股 | 东财 datacenter | 宽表（资产负债表实测 48 报告期 × 319 列） | ✅ |
| 港股 | 东财 datacenter | 长表 | ✅ |
| 美股 | 东财 datacenter | 长表（资产负债表实测 41 科目，无股本科目） | ✅ |
| 三市场 | iFinD | 未探测 | ❓ |

- 增量：按报告期，首次全量、之后拉新报告期。
- 东财 datacenter 域稳定（2026-08 实测不受行情域限流影响）。

## 4. 衍生数据获取工具 `get_financial_ratios`

### 4.1 定位

财务衍生指标（ROE/毛利率/负债率等），**原始返回、东财列名原样**（如 ROEJQ=加权ROE、ROEKCJQ=扣非ROE、MLR=毛利率、EPSJB=基本EPS、BPS=每股净资产、TOTALOPERATEREVE=营业总收入、PARENTNETPROFIT=归母净利润）。命名依据：行业标准把原始三表叫 Statements、衍生比率叫 Ratios，不叫 indicators（易误解为技术指标 MACD/RSI）。

### 4.2 参数

`code`（必填）、`periods[]`（年份列表，空=全部）。

### 4.3 数据可得性

| 市场 | 数据源 | 形态 | 状态 |
|---|---|---|---|
| A 股 | 东财 | 多期，141 列 | ✅ |
| 港股 | 东财 | **仅最新 TTM** | ⚠️ 有限 |
| 美股 | 东财 | 年报多期 | ✅ |
| 三市场 | iFinD | 未探测 | ❓ |

## 5. 公司信息获取工具 `get_company_profile`

### 5.1 定位

公司概况/分红/盈利预测，一个缓存文件（profile.json），`sections` 可选（profile/dividends/forecast，空=全部）。

### 5.2 参数

`code`（必填）、`sections[]`（profile/dividends/forecast，空=全部）。

### 5.3 数据可得性

| 内容 | A 股 | 港股 | 美股 |
|---|---|---|---|
| 概况 | ✅ 巨潮（公司名称/英文名/曾用简称/代码/入选指数） | ✅ | ❌ → SEC 公告 |
| 分红 | ✅ 历史记录（报告期/披露日/送转/现金分红比例/股息率/EPS） | ✅ | ❌ → SEC 公告 |
| 盈利预测 | ✅ 一致预期 | ✅ 分券商 | ❌ |
| iFinD 概况/分红/预测 | ❓ 未探测 | ❓ | ❓ |

美股缺失类别（概况/分红/十大股东/主营构成/IPO）需从公告原文获取——对应公告 MCP（美股走 SEC EDGAR）。

## 6. 明确不做（边界）

- ❌ 不做汇总文档工具（用户自己写脚本汇总）
- ❌ 不做跨市场科目映射（科目名原样）
- ❌ 不做实时行情（只做历史日/周/月线）
- ❌ 不做分钟级数据
- ❌ 不做汇率换算（原币返回）
- ❌ 不记录缓存失败状态（失败即提示重试）

## 7. 待办

- [ ] iFinD 财报/指标/概况探测（三市场）
- [ ] iFinD 市值历史序列能力确认；流通市值 iFinD 字段确认
- [ ] 北交所行情渠道实测（iFinD 832000.BJ / AKShare）
- [ ] 新浪港股 hfq 支持复测（记录矛盾）
- [ ] iFinD 美股成交额批量序列方案
- [ ] 腾讯数据源接入（第 3 价格回退）
- [ ] 导出文件格式确定
- [ ] 财报数值单位标注方案
- [ ] MCP 注册（Hermes config + 能力库）与 export.py 双包导出
