# 项目协作说明

操作本项目之前，先阅读本文件和 `PROJECT.md`。

## 用户目标

本项目要从“A 股公告查询工具”克制地扩展为“A 股和港股公司公告查询工具”，同时增加独立的 A 股互动问答查询。

这是个人投资研究工具，不追求大而全。优先保证数据准确、真实可用和维护简单，不为假设中的需求增加抽象或字段。

## 已确认的数据来源

- A 股公告、港股公告和 A 股互动问答均使用东方财富。
- 公告列表使用东方财富公告接口。
- 公告详情通过 `api/content/ann` 获取正文及接口实际返回的 `attach_url_web`。
- PDF 仅在实际阅读公告时下载，不在公司建档时批量下载。
- 互动问答使用东方财富股吧问答分页接口，只包含已回答问题。

## 已确认的产品要求

### 公司查询与建档工具

工具名称为 `establish_company`，不要使用 `archive_company` 等容易被理解为归档的名称。

- `establish_company(keyword, action="check")`
  - 支持公司名称、A 股代码、港股代码等关键词查询。
  - 只读，不创建缓存。
  - 忠实返回东方财富模糊搜索的前 20 条候选，不过滤、不归组、不替 AI 判断公司关系。
  - 只从东方财富返回内容中整理，不自行补充繁体名、英文名或其他推测字段。
  - 返回东方财富报告的总命中数和实际返回数；总命中数大于实际返回数时，提醒 AI 使用更精确的关键词重新查询。
- `establish_company(codes=[...], action="establish")`
  - 实际参数使用明确代码列表 `codes=[...]`，不使用逗号分隔字符串。
  - 接受一个代码，或一个 A 股代码加一个 H 股代码；不自动搜索、猜测或补全另一市场代码。
  - 一次性获取全部 A/H 公告列表。
  - 公司有 A 股时，一次性获取全部互动问答。
  - 保存公司名称和 A/H 证券代码映射。
  - 工具描述必须提醒 AI 不要使用 `-R`、`-WR` 等人民币柜台代码建档，应选择主要港股代码。
  - `establish` 已实现：保存公司映射到 `cache/companies.json`，同步 A/H 公告列表；有 A 股时同时同步互动问答。各证券独立成功或失败。

`check` 已确认的东方财富证券字段包括：

```text
Code, Name, PinYin, ID, JYS, Classify, MarketType,
SecurityTypeName, SecurityType, MktNum, TypeUS,
QuoteID, UnifiedCode, InnerCode
```

实现时可以改为项目统一的 snake_case，但不得添加上游没有返回的公司资料。

完整实施约定见 `docs/AH公告与互动问答改造方案.md`。

### 三类公司

- 纯 A 股公司：建立 A 股公告档案和互动问答档案。
- 纯港股公司：建立港股公告档案；没有 A 股互动问答。
- A+H 公司：识别并保存两个市场的证券代码及东方财富 `InnerCode`，共用同一公司档案。

公司唯一主键已定案：有 A 股时用 A 股代码，纯港股用港股代码；目录命名保持现有 `cache/{证券代码}/`，不增加前缀。`establish` 已按此实现（见 `docs/AH公告与互动问答改造方案.md` 第 4 节）。

### 公告与互动问答

- A/H 公告继续按证券代码分别保存在现有缓存目录中，查询时才在内存合并、排序和筛选。
- A/H 同一事项可能使用不同 `art_code` 和不同标题，不得擅自去重删除；原始记录都要保留。
- 互动问答单独建档，不和公告混合。
- 首次查询或建档建立完整列表，后续采用增量更新，保持现有公告档案逻辑。

## 已验证的上游边界

- 东方财富证券搜索会返回 A 股、港股、人民币柜台、权证、期货、ADR 等多种真实候选；`check` 不过滤，由 AI 根据返回字段选择。
- 用代码直接搜索可能是模糊匹配：例如 `03308` 可能同时命中 `603308`。数字代码必须先按 `Code` 精确匹配，再使用该证券返回的 `Name` 补查同名 A/H 证券。
- `Classify=HK` 也会包含港股衍生品；普通港股和人民币柜台可同时为 `TypeUS=3`。`establish` 需要校验选定代码，但 `check` 不隐藏原始候选。
- 港股证券代码可能复用。中际旭创当前港股代码 `03308` 的公告列表混入旧公司金鹰商贸集团，必须使用当前证券的 `InnerCode` 过滤，不能只按五位代码归属公告。
- 中际旭创当前映射：A 股 `300308`、H 股 `03308`；对应东方财富 `InnerCode` 分别为 `35942435293078`、`41388730200001`。
- 东方财富“点击加载更多”问答通过同域 `interface/GetData.aspx` POST 分页，不是页面 URL 的 `page=` 参数。
- PDF 地址应使用详情接口实际返回值。下载后必须检查响应确实以 `%PDF-` 开头，防止临时反爬页面被当成 PDF 缓存。

## 开发方式

- 一次只实现和验证一个小步骤，汇报真实结果后再继续。
- 不要先假设完整返回结构，再一次性实现全部功能。
- 上游接口字段、过滤规则和分页行为必须先做真实调用验证。
- 返回结构保持克制，只保留当前投资研究确实需要或东方财富实际返回的内容。
- 未经用户明确要求，不部署、不导出、不提交、不推送。
- 临时探针放在 `.codex-temp/`，不得混入生产模块。

开发测试环境：

```powershell
$env:PYTHONPATH = 'src'
& 'D:\venvs\a-share-announcements\Scripts\python.exe' -m pytest -q
```

`reference/` 仅供参考，不在其中开发。
